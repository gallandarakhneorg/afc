<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>fr.utbm.set.jasim</groupId>
		<artifactId>jasim</artifactId>
		<version>3.0-SNAPSHOT</version>
	</parent>

	<artifactId>jasim-offline-generation</artifactId>
	<name>JaSIM Offline Generation</name>

	<build>
		<plugins>
			<plugin><!-- to create a runnable jar -->
				<artifactId>maven-assembly-plugin</artifactId>
				<configuration>
					<archive>
						<manifest>
							<mainClass>fr.utbm.set.jasim.generation.JasimSimulationGenerator</mainClass>
						</manifest>
					</archive>
					<descriptorRefs>
						<descriptorRef>jar-with-dependencies</descriptorRef>
					</descriptorRefs>
				</configuration>
				<executions>
					<execution>
						<id>make-assembly</id> <!-- this is used for inheritance merges -->
						<phase>package</phase> <!-- append to the packaging phase. -->
						<goals>
							<goal>single</goal> <!-- goals == mojos -->
						</goals>
					</execution>
				</executions>
			</plugin>

		</plugins>
	</build>


	<dependencies>
		<dependency>
			<groupId>fr.utbm.set.jasim</groupId>
			<artifactId>jasim-environment3d</artifactId>
		</dependency>
		<dependency>
			<groupId>fr.utbm.set.jasim</groupId>
			<artifactId>jasim-io-base</artifactId>
		</dependency>
		<dependency>
			<groupId>fr.utbm.set.sfc.fileformat.collada</groupId>
			<artifactId>reader</artifactId>
		</dependency>
		<dependency>
			<groupId>fr.utbm.set.sfc.fileformat.collada</groupId>
			<artifactId>validator</artifactId>
		</dependency>
		<dependency>
			<groupId>fr.utbm.set.sfc.gui.swing</groupId>
			<artifactId>window</artifactId>
		</dependency>
		<dependency>
			<groupId>fr.utbm.set.sfc.gui.swing</groupId>
			<artifactId>filechooser</artifactId>
		</dependency>
		<dependency>
			<groupId>fr.utbm.set.live</groupId>
			<artifactId>entity-factory-core</artifactId>
		</dependency>
	</dependencies>

</project>
